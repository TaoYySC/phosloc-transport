# Python 3.9.x (tested on 3.9.18 in conda env phosloc); see README.md
#
# Core scientific stack
numpy==2.0.2
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
seaborn==0.13.2
PyYAML==6.0.2
joblib==1.4.2
tqdm==4.67.1

# ML models
xgboost==2.1.4
torch==2.6.0
torch-geometric==2.6.1

# Optional dependencies - uncomment only when needed, then run:
#   pip install -r requirements.txt
#
# ESM-2 embedding extraction (precompute TF_esm_embedding/; GPU recommended)
# fair-esm==2.0.1
#
# Ensembl gene annotation (Stage 3 CPTAC analysis)
# pyensembl==2.6.2
#
# Dimensionality reduction / exploratory visualization
# umap-learn==0.5.7
